void main() {
  // final setA = {1, 2, 3, 4, 5, 6};
  // final setB = {3, 4, 5, 6, 7, 8};
  // print(setA.union(setB));
  // print(setA.intersection(setB));
  // print(setA.difference(setB));
  // print(setA..addAll(setB));
  final myList = ['a', 'b', 'c'];
  print(myList);
  for (var element in myList) {
    print(element);
  }
  final reverhod = myList.reversed;
  print(reverhod);

  // print("Hello, World!");
  // final set = {'one', 'two', 'three', 'four'};
  // print(set);
  // set.add('five');
  // print(set);
  // print(set.contains('four'));
  // set.remove('four');
  // print(set.contains('six'));
  // print([1, 2, 3]);
}
