import 'package:flutter/material.dart';

class NoConnecctionPage extends StatelessWidget {
  const NoConnecctionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
